{
  "name": "startup-analyser",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd kore-frontend && npm install && npm run build",
    "start": "cd kore-frontend && npm start"
  }
}
